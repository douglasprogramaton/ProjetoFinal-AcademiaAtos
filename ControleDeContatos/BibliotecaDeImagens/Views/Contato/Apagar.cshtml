@model ContatoModel
@{
    
    ViewData["Title"] = "Apagar contato";
}

<div class="text-center">
    <h3>Realmente deseja apagar o contato(@Model.Name)?</h3>
</div>

<a class="btn btn-primary" asp-controller="Contato" asp-action="Index" role="button">Cancelar</a>
<a class="btn btn-danger"  asp-route-id="@Model.Id" asp-controller="Contato" asp-action="Apagarconfirmar" role="button">Confirmar</a>
